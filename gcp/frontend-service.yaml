apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: formulario-academico-front
spec:
  template:
    spec:
      containers:
        - image: REGION-docker.pkg.dev/PROJECT_ID/formulario-academico/frontend:latest
          ports:
            - containerPort: 8080
